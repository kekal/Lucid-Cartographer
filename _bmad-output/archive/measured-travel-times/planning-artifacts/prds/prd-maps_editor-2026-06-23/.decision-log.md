# Decision Log — Measured Travel-Time & Distance Estimation PRD

Canonical memory + audit trail. Every decision, change, and override is recorded here as the conversation unfolds.

## 2026-06-23

- **Intent:** Create (no prior PRD found). Topic: measured travel-time / road-distance estimation, replacing the manual self-hosted OSRM pipeline.
- **Primary input:** `_bmad-output/planning-artifacts/research/technical-travel-time-distance-estimation-research-2026-06-23.md` (technical research, decision already reached: D smart-haversine default + E Valhalla measured provider; deprecate OSRM; Itinero fallback only).
- **Workspace bound:** `_bmad-output/planning-artifacts/prds/prd-maps_editor-2026-06-23/`.
- **Stakes:** Shipping product feature → full rigor (NFRs, success metrics, migration/deprecation handling).
- **Working mode:** Fast path → batch gaps, draft full PRD with `[ASSUMPTION]` tags, iterate.

### Fast-path decisions (2026-06-23)
- **OSRM fate → REMOVE OUTRIGHT.** Delete `OsrmTravelTimeProvider`, its config/options, the compose `osrm` profile/services, and `docs/osrm.md`. Valhalla fully supersedes. This is a breaking change for any operator currently on `TravelTime:Provider=Osrm` — PRD must call this out + give a migration note. (Supersedes research option "keep OSRM as legacy if desired".)
- **Smart-haversine correction factors → TUNABLE CONFIG + SANE DEFAULTS.** Ship documented per-mode detour factors (e.g. drive ≈ ×1.3) + per-mode speeds as defaults, exposed via config for operator tuning. Exact values are `[ASSUMPTION]` pending empirical tuning (research open question).
- **Valhalla config surface → CONFIG / ENV ONLY.** `TravelTime:Provider=Valhalla` + compose `valhalla` profile + `tile_urls` env, matching today's Mock/OSRM selection pattern. No new admin UI in scope.
- **Success metrics → ALL FOUR headline measures:** (1) turnkey footprint (1 container + 1 env var), (2) privacy preserved NFR7 (zero coord egress, verified), (3) accuracy uplift (measured >> haversine; smart-haversine > raw straight line), (4) clean fidelity ladder (estimate → good estimate → measured, correctly badged).

### Draft authored (2026-06-23)
- Grounded FRs in the live seam via an Explore pass. **Key correction:** `MockTravelTimeProvider` ALREADY applies per-mode *speeds*; "smart-haversine" = adding per-mode *detour/winding factors* on top (FR-1). Fidelity badges (Estimated/Measured/Manual/Placeholder) already exist → no new badge type; smart-haversine improves the Estimated rung (FR-17). Decision: ladder is two visible badges, not three.
- PRD written: 5 feature groups (A smart-haversine, B Valhalla provider, C turnkey deploy, D OSRM removal+migration, E badging), 17 FRs, full NFR set (NFR7 hard, NFR8 attribution, perf, reliability, units, build discipline, DI seam), migration/deprecation section, success metrics + counter-metrics.
- Technical-how moved to `addendum.md` (seam as-built, Valhalla costing mapping auto/pedestrian/bicycle + JSON differences, compose snippet, OSRM deletion targets, rejected-options provenance A/B/C).
- Open questions logged OQ-1..OQ-7 (factor values, Valhalla accuracy, tile build cost, unknown-provider behavior FR-15, keep-vs-invalidate OSRM cache rows FR-16, two-badge ladder FR-17, image pinning OQ-7). Status: draft, awaiting user review before Finalize.

### Finalize (2026-06-23)
- **User chose: finalize.** Ran the full sequence.
- **1. Decision-log audit:** all prior decisions captured in PRD/addendum. ✔
- **2. Input reconciliation** (research doc vs prd+addendum → `reconcile-research.md`): faithful; one MED fixed (three-rung conceptual ladder was compressed to two in prose — §1 summary corrected to "raw straight-line → smart estimate → measured", two badges). Walk/Cycle factors noted as PRD-introduced in OQ-1.
- **3. Reviewer gate** (Create intent → findings in-conversation, no rendered report): rubric walker → STRONG (7 dims strong/adequate, 0 critical) `review-rubric.md`; adversarial → "don't sign off as-is", 3 critical / 6 high `review-adversarial-general.md`.
- **4. Findings resolved (autofixed into PRD):**
  - C1 NFR7 asserted-not-designed → added Containment + Verification-method sub-clauses to NFR7.
  - C2 first-boot/tile-build window undefined → added **FR-13a** (degrade to Estimated + operator-visible signal + recompute on reachability).
  - C3 SM-3 untestable → reframed to validation-method + thresholds T₁/T₂ (set in impl, OQ-2).
  - H1 silent Measured→Estimated downgrade → FR-15 now requires a **prominent** warning + release-note callout.
  - H2 stale kept OSRM rows + never-downgrade guard → **OQ-5 leaning reversed keep→invalidate**; FR-16 rewritten; addendum recompute-trigger nuance added.
  - Rubric MED: NFRs numbered (NFR7, NFR8, NFR-9..NFR-13).
- **New open questions added:** OQ-8 (tile-build signal depth), OQ-9 (NFR7 verification depth).
- **Triage:** no phase-blockers — every OQ has a stated default; PRD safe for architecture/epics. All OQs deferred to implementation phase, dispositions in the §10 table.
- **5. Polish:** structural+prose pass; fixed §8 contradiction (old "rows remain valid" → invalidate, consistent with revised FR-16); aligned warning wording.
- **6. External handoffs:** none configured. **7. Closed:** `status: final`.
- **Reviewer artifacts retained:** `review-rubric.md`, `review-adversarial-general.md`, `reconcile-research.md`.
