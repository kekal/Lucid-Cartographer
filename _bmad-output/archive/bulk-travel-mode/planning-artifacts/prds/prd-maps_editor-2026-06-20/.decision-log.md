# Decision Log — Bulk Travel Mode Assignment PRD

Canonical memory + audit trail. Every decision, change, and override is recorded here.

## 2026-06-20

- **Intent:** Create (no prior PRD in `prds/`).
- **Language:** PRD authored in English (project convention: `document_output_language: English`, all `docs/` + archived PRDs in English). Conversation in Russian per user's evident preference.
- **Primary input:** `_bmad-output/bulk-travel-mode/planning-artifacts/requirements.md` (detailed requirements draft authored earlier this session) + direct source-code investigation of the Trip View travel-mode subsystem.
- **Pre-captured clarifications (AskUserQuestion this session):**
  - Overwrite behavior → a **checkbox** beside the selector lets the user choose (overwrite all vs fill-empty-only).
  - Mode set → **Drive / Walk / Cycle + Any/Air**.
  - Placement → **header of the Trip stops panel** (next to Sort / Recompute).
- **Stakes:** internal single-operator tool, self-hosted; compact PRD (~5 pages), no journey/persona sections.
- **Working mode:** Fast path — full draft assembled from inputs with `[ASSUMPTION]` tags, then iterate.
- **Assumptions made during Fast-path draft (to confirm at Finalize):**
  - A1 (was Q1): bulk selector is disabled while `IsAnyLegComputing` is true, mirroring the Sort/Recompute buttons.
  - A2 (was Q2): mobile-panel (`MobileTripPanel.razor`) mirroring deferred to tech-debt (consistent with prior mirror-to-mobile defer).
  - A3 (was Q3): the Any/Air "time becomes —" hint is optional and deferred.
  - A4: control sits inline in the existing header action row (no new full-width row).
  - A5: selector defaults to an unselected placeholder so opening it is a deliberate act (no accidental mass-change).

### Finalize (2026-06-20)

- **Step 1 — Decision-log audit:** all decisions/assumptions captured; no orphans.
- **Step 2 — Input reconciliation** (`reconcile-requirements.md`): no high-severity gaps. 3 low gaps fixed — NFR-3 Manual/Measured linkage restored; FR-8 "ground cache rows not recomputed" restored; A2 marked index-only deferral.
- **Step 3 — Reviewer gate** (`review-rubric.md`): grade **Excellent** (all dimensions strong/adequate; 0 critical/high; 2 medium, 4 low). All findings autofixed:
  - M1 → `[NOTE FOR PM]` at FR-6 + tightened AC-7 wording + new open item **A6** (confirm-on-overwrite-on; default: no confirm).
  - M2 → new **FR-13** (disable control during `IsAnyLegComputing`) + **AC-9**; A1 now points to FR-13.
  - Low → NFR-5 rewritten to concrete bound (single write txn, one refresh); inline `[ASSUMPTION]` round-trip fixed for A1/A2; added **§1.1 Glossary** (6 terms); normalized "Any-Air" → "Any/Air" in §1/§3.
- **Step 4 — Triage:** no phase-blockers. Deferred non-blockers: A2 (mobile mirror, tech-debt), A3 (Any/Air hint), A4/A5 (UX confirm), A6 (product/UX confirm). All logged with disposition.
- **Step 5 — Polish:** light structural/prose pass; PRD already graded Excellent, no restructuring needed.
- **Step 6 — External handoffs:** none configured (`external_handoffs` empty).
- **Step 7 — Close:** `status: final`, `updated: 2026-06-20`.
