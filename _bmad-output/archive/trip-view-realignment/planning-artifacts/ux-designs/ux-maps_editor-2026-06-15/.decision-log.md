# UX Decision Log — Trip View realignment

Canonical memory for this UX run. Spines distilled at Finalize.

## 2026-06-15
- **Intent:** scoped **delta** UX for the Trip View realignment. Source: finalized PRD `prds/prd-maps_editor-2026-06-15` + shipped spines `ux-maps_editor-2026-06-11` (both `status: final`). Working mode: **Fast path**.
- **First task (Yurik):** "check ux changes in current PRD" → produced `.working/ux-change-inventory.md` (full delta vs shipped spines: 11 component changes incl. 2 NEW, 4 input-affordance changes, 3 state changes incl. 2 NEW, 2 new interaction primitives, DESIGN.md component additions).
- **Carry-in artifact:** the inter-row-connector mockup rendered during the PRD session (trip table + connector + full columns) — reference for the new layout.
- Design system (colors/type/spacing tokens) **unchanged** — this is a behavioral + component delta, not a re-skin.
- **Open UX decisions resolved (Yurik, Fast path batch):**
  1. Per-leg **mode control** = **pill that opens a menu** (compact; "Drive" / "Any — set mode" → Walk/Drive/Cycle/Any-Air picker).
  2. Leg-time **edit = inline** on the connector; small **reset (↺)** clears to auto.
  3. **Pickers = native** inputs (`datetime-local` / `time`), token-styled — accessible, locale-aware, no new components.
  4. **Connector density = single compact line** (mode · time · distance · fidelity); **reset (↺) appears on hover/focus only**.
- Spine deltas drafted (Fast path) with [ASSUMPTION] tags where inferred beyond the PRD.
- **Key-screen mock** rendered + saved → `mockups/trip-view-desktop.html` (wide table, inter-row connector with reset-on-hover, mode pill, "Any — set mode", native pickers, dated "Finish" footer). Standalone (explicit token hex) so it opens directly.
- **Mock coverage:** desktop key screen done — the surface where layout drives behavior. Mobile mock deferred to the mirror phase. No other desktop surface needs a visual reference (spine tables suffice).
- **Reviewer gate:** skipped (Yurik) — internal stakes; PRD already audited.
- **FINALIZED 2026-06-15.** Both spines `status: final` (delta, inherit `ux-maps_editor-2026-06-11`, win on conflict for Trip View). Next: `bmad-create-architecture` (Feature F migration + MCP) or `bmad-create-epics-and-stories`.
