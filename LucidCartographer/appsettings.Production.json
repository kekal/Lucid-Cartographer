{
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Auth": {
    "BypassLocalAddresses": false,
    "TrustedProxies": [
      "127.0.0.1",
      "::1"
    ]
  },
  "Mcp": {
    "//AllowLocalNetworkBypass": "false in Production: the connecting peer behind a reverse proxy/tunnel is an RFC1918 address, so the local bypass would otherwise expose /mcp to the world. Remote callers must present MCP_API_KEY.",
    "AllowLocalNetworkBypass": false
  },
  "Enrichment": {
    "Concurrency": 4,
    "MaxConcurrentPages": 8,
    "BatchSize": 16,
    "IdlePollSeconds": 30,
    "MaxRetries": 5,
    "BackoffBaseSeconds": 30
  }
}
